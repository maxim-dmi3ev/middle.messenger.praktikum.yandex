@import "../../styles/colors.styl";

.avatar {
  width: 60px;
  height: 60px;
  border-radius: 50%;
  background-color: $background-grey1;

  img {
    width: 100%;
    display: block;
    border-radius: 50%;
  }
}
