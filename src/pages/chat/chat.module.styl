@import "../../styles/colors.styl";

.root {
  display: flex;
}

.sidebar {
  flex: 0 0 450px;
  border-right: 1px solid $border-default;
}

.main {
  flex: 1 1 auto;
  display: flex;
  justify-content: center;
  align-items: center;
  font-size: 20px;
}
